[package]
name = "imu"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "Interface for interacting with various IMU devices"
license.workspace = true
repository.workspace = true
readme = "README.md"

[lib]
name = "imu"
crate-type = ["rlib"]

[dependencies]
log = "0.4"
bno055 = { path = "bno055", optional = true }
bmi088 = { path = "bmi088", optional = true }
hiwonder = { path = "hiwonder", optional = true }
hexmove = { path = "hexmove", optional = true }

[features]
default = []
# Define features that enable the optional dependencies
bno055 = ["dep:bno055"]
bmi088 = ["dep:bmi088"]
hiwonder = ["dep:hiwonder"]
hexmove = ["dep:hexmove"]
# Optional: A feature to enable all IMUs
full = ["bno055", "bmi088", "hiwonder", "hexmove"]
